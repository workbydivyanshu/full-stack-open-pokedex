name: Hello world action

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  hello_world_job:
    runs-on: ubuntu-20.04
    steps:
      - name: Say hello
        run: echo "Hello World!"
      - name: Show date
        run: date
      - name: List directory contents
        run: ls -l
